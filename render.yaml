services:
  - type: web
    name: housemate-zm
    runtime: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: housemate-data
      mountPath: /opt/render/project/src/data
      sizeGB: 1
